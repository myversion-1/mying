# Lighthouse 测试快速开始 (Quick Start Guide)

**更新日期：** 2025-01-06

---

## 🚀 三步启动 Lighthouse 测试

### 步骤 1: 启动开发服务器

在**第一个终端窗口**中：

```powershell
cd "C:\Users\Administrator\Desktop\Miying Web\mying-web"
npm run dev
```

**等待看到：**
```
✓ Ready in X.Xs
○ Local:        http://localhost:3000
```

### 步骤 2: 验证服务器运行

在浏览器中打开：
- `http://localhost:3000/products`

如果页面正常加载，说明服务器已就绪。

### 步骤 3: 运行 Lighthouse 测试

在**第二个终端窗口**（新窗口）中：

```powershell
cd "C:\Users\Administrator\Desktop\Miying Web\mying-web"
npm run lighthouse:quick
```

---

## ⚡ 一键命令（如果服务器已运行）

如果服务器已经在运行，直接运行：

```powershell
npm run lighthouse:quick
```

---

## 🔍 故障排除

### 问题：服务器没有响应

**检查服务器状态：**
```powershell
# 检查端口
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue

# 测试连接
Invoke-WebRequest -Uri "http://localhost:3000" -Method Head -TimeoutSec 2
```

**解决方案：**
1. 确保服务器已完全启动（看到 "Ready" 消息）
2. 检查防火墙设置
3. 尝试重启服务器

### 问题：端口被占用

**终止占用端口的进程：**
```powershell
npm run kill:nextjs
```

然后重新启动服务器。

---

## 📊 测试结果

测试完成后，报告保存在：
```
lighthouse-reports/
├── before.json
├── after.json
└── comparison.json
```

查看结果：
```powershell
npm run lighthouse:compare
```

---

## 💡 提示

- **使用两个终端窗口：** 一个运行服务器，一个运行测试
- **等待服务器就绪：** 看到 "Ready" 消息后再运行测试
- **检查浏览器：** 确保可以访问 `http://localhost:3000`

---

**快速开始指南**  
**状态：** ✅ 测试工具已配置  
**下一步：** 启动服务器并运行测试


























